# Copia a .env para docker compose up

FRONTEND_PORT=3000

# Con el proxy de nginx, VITE_API_URL es la URL pública del frontend + /api
# En Dokploy: http://IP_SERVIDOR:5446/api
# En local con el compose del frontend solo: http://localhost:3000/api
VITE_API_URL=http://localhost:3000/api
