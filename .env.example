# Copia a .env (no se sube a git).

# URL del API (https + /api al final).
# En Docker/Dokploy se pasa como build arg: --build-arg VITE_API_URL=https://api.tudominio.com/api
VITE_API_URL=http://localhost:8080/api
